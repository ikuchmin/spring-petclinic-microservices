apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-pc-customer-db-password
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  db-password: {{ .Values.dbs.pc_customer.password | b64enc }}
