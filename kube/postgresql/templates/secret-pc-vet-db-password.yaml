apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app.fullname" . }}-pc-vet-db-password
  labels:
    {{- include "app.labels" . | nindent 4 }}
type: Opaque
data:
  db-password: {{ .Values.dbs.pc_vet.password | b64enc }}
